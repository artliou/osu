<html>
{{#each foo}} {{this}} {{/each}}
<h3>Add new actor:</h3>
<form id="addactor" action="/actor" method="post">
    First name:
    <input type="text" name="fname"> Last name:
    <input type="text" name="lname">
    <br>
    <input type="submit" value="Submit">
</form>
<br/>
<h3>Current Actors:</h3>
<table>
    <thead>
        <th>First Name</th>
        <th>Last Name</th>
    </thead>
    <tbody>
        {{#each actors}}
        <tr>
            <td>{{first_name}}</td>
            <td>{{last_name}}</td>
            <td>
                <button onclick="deleteActor({{id}})">Delete</button>
            </td>
            <td>
                <a href="/actor/{{id}}">Update</a>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>

<br/>

</html>